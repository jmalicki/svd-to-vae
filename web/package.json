{
  "name": "svd-grad-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "gen:ringing": "node scripts/gen-ringing-plot.mjs",
    "predev": "npm run gen:ringing",
    "dev": "vite",
    "prebuild": "npm run gen:ringing",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test:ringing": "npm run gen:ringing"
  },
  "dependencies": {
    "js-pytorch": "^0.7.2",
    "ml-matrix": "^6.12.1"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^6.3.5"
  }
}
